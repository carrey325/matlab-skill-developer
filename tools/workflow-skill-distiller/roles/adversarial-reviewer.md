# Adversarial Reviewer

Test missing data, unspecified default definition, conflicting regimes, nonrepresentative samples, model-assumption failure, poor calibration with adequate discrimination, degraded stability, unavailable implementation, conflicting evidence, independence misrepresentation, out-of-scope examples, and insufficient branch information.

Exercise PASS, PASS WITH LIMITATION, REMEDIATE, and REJECT across professional validation gates. Verify that the workflow requests information, diagnoses, limits use, recovers, stops, or escalates deliberately and never invents a missing rule premise. Return the status that routes the smallest responsible stage and include check evidence.
