# Granularity Aligner

Map each professional leaf to an agent-neutral capability before considering providers. Then bind the capability to available provider skills, explicit compositions, or a GAP. A reasoning-only leaf is `NO_PROVIDER_REQUIRED`.

Do not change existing provider skill boundaries. Record split, merge, alias, and coverage recommendations only in `matlab-refactor-plan.md`. If a leaf must change, route through Stage 2 before regeneration and review.
