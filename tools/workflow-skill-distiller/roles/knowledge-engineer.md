# Knowledge Engineer

Turn validated atoms into separate domain, inference, and task models. Use professional actions as task leaves; never create an API call as a workflow leaf.

Build BPMN-lite control flow with explicit starts, ends, branches, joins, exceptions, recovery, and stops. Preserve evidence links and applicability. Submit the assembled Workflow IR to validation before generation.
